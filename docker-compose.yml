services:
  synapse-user-manager:
    build: .
    container_name: synapse-user-manager
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./data:/app/data
